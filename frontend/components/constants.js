const RivalsServer = 'ws://localhost:8080';
const RivalsServerPort = 8080;
const FrontendPort = 3000; // Add this line

module.exports = {
  RivalsServer,
  RivalsServerPort,
  FrontendPort, // Add this line
};
